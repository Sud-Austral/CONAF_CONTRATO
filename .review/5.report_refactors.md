# 🛠️ Plan de Refactorización: refactor-suggester

## 🎯 RESUMEN DE MEJORAS PRIORIZADAS
Este plan transforma los riesgos críticos identificados en un camino de implementación segura, enfocándose primero en la integridad funcional y seguridad.

---

### 1️⃣ QUICK WINS (P0/P1) — < 30 min por pieza

**🔨 Refactor**: Corrección de llamada a `contratosService.create`
**🎯 Objetivo**: Resolver error de runtime en la creación de contratos (`1.report_errors.md`).
**💻 Código (Before/After)**: 
```javascript
// BEFORE (en ContractDrawer.jsx)
await contratosService.create(payload);
// AFTER
await contratosService.create(employee.id, selectedTemplate || 'contrato_indefinido', payload.datos_formulario);
```
**⚠️ Riesgo**: Bajo.
**⏱️ Esfuerzo**: XS (5 min).

**🔨 Refactor**: Eliminación de `data.json.gz` público
**🎯 Objetivo**: Resolver riesgo de cumplimiento crítico (`2.report_compliance.md`).
**💻 Acción**: Borrar `public/data.json.gz`. Asegurar que el frontend consuma únicamente del servicio `empleadosService.list` el cual redirige a la API auténticada.
**⚠️ Riesgo**: Bajo (siempre que la API esté operativa).
**⏱️ Esfuerzo**: XS (2 min).

**🔨 Refactor**: Soporte de Templates en Drawer
**🎯 Objetivo**: Cumplir con requerimiento de API v2.0 y mejorar UX (`INSUMO/CONAF_API_FRONTEND_DOC.md`).
**💻 Acción**: Añadir selector de plantillas en `ContractSection` alimentado por `contratosService.getTemplates`.
**⚠️ Riesgo**: Medio (cambio visual).
**⏱️ Esfuerzo**: S (20 min).

---

### 2️⃣ MEJORAS DE CALIDAD (P2)

**🔨 Refactor**: Centralización de Estados de Contrato (Constants)
**🎯 Objetivo**: Reducir duplicidad de strings mágicos (`3.report_code_quality.md`).
**💻 Código (After)**: `export const CONTRACT_STATES = { PENDIENTE: 'PENDIENTE', ... };`
**⚠️ Riesgo**: Bajo (requiere búsqueda y reemplazo global).
**⏱️ Esfuerzo**: M (1 hora).

**🔨 Refactor**: Mobile-Safe Tab Bar
**🎯 Objetivo**: Optimizar navegación en móviles (`6.report_mobile.md`).
**💻 Código (After)**: `<div className="overflow-x-auto scrollbar-none ... hover:overflow-x-scroll"> ... </div>`
**⚠️ Riesgo**: Bajo.
**⏱️ Esfuerzo**: S (15 min).

---

### 3️⃣ EVOLUCIÓN ARQUITECTÓNICA (P3)

**🔨 Refactor**: Migración a Context-Based Contract State
**🎯 Objetivo**: Desacoplar Drawer de la lógica de negocio (`4.report_architecture.md`).
**💻 Acción**: Crear `src/context/ContractWorkflowContext.jsx`.
**⚠️ Riesgo**: Alto (cambio estructural).
**⏱️ Esfuerzo**: L (4-8 horas).
